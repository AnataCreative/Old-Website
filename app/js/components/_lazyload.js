/* ==========================================================================
   AnataCreative - headerScroll
   ========================================================================== */

var anatacreative = anatacreative || {};

anatacreative.lazyload = function(undefined) {

    // Init
    var init = function() {
        var lazyloadImages = new LazyLoad({
            elements_selector: ".js-lazyload-img"
        });

    }();
};
